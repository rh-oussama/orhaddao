apiVersion: v1
kind: Service
metadata:
  name: playground-svc
  namespace: dev
spec:
  type: NodePort
  selector:
    app: playground-app        
  ports:
    - port: 8888
      targetPort: 8888  
      nodePort: 8888